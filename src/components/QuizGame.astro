---
import type { ExerciseItem, Level } from '../types/exercise';

interface Props {
  items: ExerciseItem[];
  level: Level;
  title: string;
}

const { items, level, title } = Astro.props;
const quizId = `quiz-${Math.random().toString(36).slice(2, 9)}`;
---

<div class="quiz-game" id={quizId}>
  <div class="quiz-header">
    <h2 class="quiz-title">🎯 {title}</h2>
    <div class="quiz-progress">
      <div class="quiz-progress-bar">
        <div class="quiz-progress-fill"></div>
      </div>
      <span class="quiz-progress-text">1 / {items.length}</span>
    </div>
  </div>
  
  <div class="quiz-content">
    <div class="quiz-question-box">
      <p class="quiz-question"></p>
    </div>
    
    <div class="quiz-answers"></div>
    
    <div class="quiz-feedback" hidden>
      <span class="feedback-icon"></span>
      <span class="feedback-text"></span>
    </div>
  </div>
  
  <div class="quiz-score" hidden>
    <span class="score-emoji">🎉</span>
    <p class="score-value"></p>
    <p class="score-message"></p>
    <button type="button" class="btn btn--primary btn--lg replay-btn">🔄 Rejouer</button>
  </div>
</div>

<script define:vars={{ items, quizId }}>
  const container = document.getElementById(quizId);
  if (!container) throw new Error('Quiz container not found');

  let currentIndex = 0;
  let score = 0;
  let hasAnswered = false;
  
  const shuffledItems = [...items].sort(() => Math.random() - 0.5).slice(0, 10);
  
  const progressFill = container.querySelector('.quiz-progress-fill');
  const progressText = container.querySelector('.quiz-progress-text');
  const questionEl = container.querySelector('.quiz-question');
  const answersEl = container.querySelector('.quiz-answers');
  const feedbackEl = container.querySelector('.quiz-feedback');
  const scoreEl = container.querySelector('.quiz-score');
  const contentEl = container.querySelector('.quiz-content');

  function generateOptions(correct, allItems) {
    const opts = new Set([correct]);
    const num = parseInt(correct);
    
    if (!isNaN(num)) {
      while (opts.size < 4) {
        const delta = Math.floor(Math.random() * 6) - 3;
        if (delta !== 0) opts.add(String(num + delta));
      }
    } else {
      const others = allItems.map(i => i.a).filter(a => a !== correct);
      others.sort(() => Math.random() - 0.5);
      for (const o of others) {
        if (opts.size >= 4) break;
        opts.add(o);
      }
    }
    return [...opts].sort(() => Math.random() - 0.5);
  }

  function showQuestion() {
    const item = shuffledItems[currentIndex];
    if (!item) return showScore();
    
    hasAnswered = false;
    const progress = ((currentIndex) / shuffledItems.length) * 100;
    progressFill.style.width = `${progress}%`;
    progressText.textContent = `${currentIndex + 1} / ${shuffledItems.length}`;
    questionEl.textContent = item.q;
    feedbackEl.hidden = true;
    
    const options = item.options || generateOptions(item.a, shuffledItems);
    answersEl.innerHTML = options.map(opt => 
      `<button class="quiz-answer" data-value="${opt}">${opt}</button>`
    ).join('');
    
    answersEl.querySelectorAll('.quiz-answer').forEach(btn => {
      btn.addEventListener('click', () => handleAnswer(btn, item.a));
    });
  }

  function handleAnswer(btn, correct) {
    if (hasAnswered) return;
    hasAnswered = true;
    
    const value = btn.dataset.value;
    const isCorrect = value === correct;
    
    if (isCorrect) {
      score++;
      btn.classList.add('correct');
      feedbackEl.querySelector('.feedback-icon').textContent = '✅';
      feedbackEl.querySelector('.feedback-text').textContent = 'Bravo !';
    } else {
      btn.classList.add('incorrect');
      answersEl.querySelectorAll('.quiz-answer').forEach(b => {
        if (b.dataset.value === correct) b.classList.add('correct');
      });
      feedbackEl.querySelector('.feedback-icon').textContent = '❌';
      feedbackEl.querySelector('.feedback-text').textContent = `La réponse était : ${correct}`;
    }
    
    feedbackEl.hidden = false;
    
    setTimeout(() => {
      currentIndex++;
      if (currentIndex < shuffledItems.length) {
        showQuestion();
      } else {
        showScore();
      }
    }, 1500);
  }

  function showScore() {
    contentEl.hidden = true;
    scoreEl.hidden = false;
    progressFill.style.width = '100%';
    
    const pct = Math.round((score / shuffledItems.length) * 100);
    scoreEl.querySelector('.score-value').textContent = `${score} / ${shuffledItems.length}`;
    
    let emoji = '🎉';
    let msg = 'Super champion !';
    if (pct < 50) { emoji = '💪'; msg = 'Continue, tu vas y arriver !'; }
    else if (pct < 80) { emoji = '👍'; msg = 'Bien joué !'; }
    
    scoreEl.querySelector('.score-emoji').textContent = emoji;
    scoreEl.querySelector('.score-message').textContent = msg;
  }

  container.querySelector('.replay-btn')?.addEventListener('click', () => {
    currentIndex = 0;
    score = 0;
    shuffledItems.sort(() => Math.random() - 0.5);
    contentEl.hidden = false;
    scoreEl.hidden = true;
    showQuestion();
  });

  showQuestion();
</script>

<style>
  .quiz-game {
    background: var(--color-bg-card);
    border-radius: var(--radius-lg);
    padding: 2rem;
    box-shadow: var(--shadow-md);
    max-width: 600px;
    margin: 0 auto;
  }
  
  .quiz-header {
    margin-bottom: 1.5rem;
  }
  
  .quiz-title {
    font-family: var(--font-display);
    font-size: 1.5rem;
    text-align: center;
    margin-bottom: 1rem;
  }
  
  .quiz-progress {
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  
  .quiz-progress-bar {
    flex: 1;
    height: 12px;
    background: #e2e8f0;
    border-radius: var(--radius-full);
    overflow: hidden;
  }
  
  .quiz-progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--color-primary), var(--color-primary-light));
    transition: width 0.3s ease;
    width: 0%;
  }
  
  .quiz-progress-text {
    font-weight: 700;
    color: var(--color-text-muted);
    font-size: 0.9rem;
    min-width: 60px;
  }
  
  .quiz-question-box {
    padding: 1.5rem;
    background: linear-gradient(135deg, #fef3c7, #fce7f3);
    border-radius: var(--radius-md);
    margin-bottom: 1.5rem;
    text-align: center;
  }
  
  .quiz-question {
    font-family: var(--font-display);
    font-size: 1.5rem;
    margin: 0;
  }
  
  .quiz-answers {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }
  
  .quiz-answer {
    padding: 1.25rem;
    background: #f8fafc;
    border: 3px solid #e2e8f0;
    border-radius: var(--radius-md);
    font-size: 1.1rem;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  
  .quiz-answer:hover:not(.correct):not(.incorrect) {
    border-color: var(--color-primary);
    background: white;
    transform: scale(1.02);
  }
  
  .quiz-answer.correct {
    border-color: var(--color-success);
    background: #dcfce7;
    animation: pulse 0.5s ease;
  }
  
  .quiz-answer.incorrect {
    border-color: var(--color-error);
    background: #fee2e2;
    animation: shake 0.5s ease;
  }
  
  .quiz-feedback {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
    padding: 1rem;
    border-radius: var(--radius-md);
    font-weight: 600;
  }
  
  .quiz-score {
    text-align: center;
    padding: 2rem;
  }
  
  .quiz-score .score-emoji {
    font-size: 4rem;
    display: block;
    margin-bottom: 1rem;
  }
  
  .quiz-score .score-value {
    font-family: var(--font-display);
    font-size: 2.5rem;
    color: var(--color-primary);
    margin: 0 0 0.5rem;
  }
  
  .quiz-score .score-message {
    color: var(--color-text-muted);
    font-size: 1.1rem;
    margin: 0 0 1.5rem;
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
  
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-5px); }
    75% { transform: translateX(5px); }
  }
  
  @media (max-width: 480px) {
    .quiz-answers {
      grid-template-columns: 1fr;
    }
  }
</style>
